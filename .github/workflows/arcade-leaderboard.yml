name: Arcade leaderboard

# Re-verifies submitted mini-game scores (issues labelled "arcade-score" or
# containing a gsmg-arcade block) by RE-SIMULATING the move-log, then rebuilds
# the public arcade leaderboard. Also runnable manually to refresh the board.
on:
  issues:
    types: [opened, edited, labeled]
  workflow_dispatch:

permissions:
  contents: write
  issues: write

concurrency:
  group: gsmg-arcade
  cancel-in-progress: false

jobs:
  verify:
    if: >
      github.event_name == 'workflow_dispatch' ||
      contains(github.event.issue.body, 'gsmg-arcade') ||
      contains(join(github.event.issue.labels.*.name, ','), 'arcade-score')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Re-simulate scores and rebuild arcade leaderboard
        run: node .github/scripts/verify-arcade.mjs

      - name: Commit updated data
        run: |
          git config user.name  "gsmg-bot"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/
          if git diff --cached --quiet; then
            echo "No data changes."
          else
            git commit -m "arcade: update from issue #${{ github.event.issue.number }}"
            git push
          fi

      - name: Comment on the issue and close it
        if: github.event_name == 'issues'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            let body = "Processed — thanks!";
            try { body = fs.readFileSync('arcade-summary.txt', 'utf8'); } catch (e) {}
            await github.rest.issues.createComment({
              owner: context.repo.owner, repo: context.repo.repo,
              issue_number: context.issue.number, body
            });
            await github.rest.issues.update({
              owner: context.repo.owner, repo: context.repo.repo,
              issue_number: context.issue.number, state: 'closed'
            });
